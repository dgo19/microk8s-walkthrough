apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-zabbix-web
spec:
spec:
  rules:
  - host: zabbix-test.microk8s.local
    http:
      paths:
      - backend:
          serviceName: zabbix-web-nginx-pgsql
          servicePort: 8080
